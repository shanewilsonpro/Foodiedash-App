// Libraries
import { StyleSheet } from "react-native";

// Theme
import { SPACES } from "../../../../theme/theme";

export const styles = StyleSheet.create({
  List: {
    paddingVertical: SPACES.padding,
  },
  ListContainer: {
    gap: SPACES.gap,
    paddingHorizontal: SPACES.padding,
  },
});